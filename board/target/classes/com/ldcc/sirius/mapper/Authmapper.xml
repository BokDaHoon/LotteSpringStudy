<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ldcc.sirius.dao.IAuthDao">

	<select id="checkExistId" resultType="integer" parameterType="java.lang.String">
		SELECT COUNT(*)
		FROM auth
		WHERE auth_id = #{id}
	</select>

	<select id="checkAuth" resultType="java.lang.String" parameterType="java.lang.String">
		SELECT auth_pw AS pw
		FROM auth
		WHERE auth_id = #{id}
	</select>

	<insert id="register" parameterType="com.ldcc.sirius.vo.Auth">
		insert into auth (auth_id, auth_name, auth_pw) values
		(#{id}, #{name}, #{pw})
	</insert>
	
	<select id="getOne" resultType="com.ldcc.sirius.vo.Auth" parameterType="java.lang.String">
		SELECT auth_id AS id, auth_pw AS pw, auth_name AS name
		FROM auth
		WHERE auth_id = #{id}
	</select>

</mapper>